apiVersion: apps/v1
kind: Deployment
metadata:
  name: premium-app
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: premium-app
  template:
    metadata:
      labels:
        app: premium-app
    spec:
      containers:
      - image: deampuleadd/premium-app:k8s
        imagePullPolicy: Always
        name: php-fpm
        volumeMounts:
        - mountPath: /var/www/html/.env
          name: env-secret-volume
          subPath: .env
        - mountPath: /var/www/html/public/images
          name: nfs-storage
          subPath: images
      - image: deampuleadd/custom-nginx:k8s
        imagePullPolicy: Always
        name: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /var/www/html/public/images
          name: nfs-storage
          subPath: images
      volumes:
      - name: env-secret-volume
        secret:
          secretName: sealed-env-secret
      - name: nfs-storage
        persistentVolumeClaim:
          claimName: nfs-main-pvc